---
version: 1
slug: "src-app-tsx"
primary_target: "src/App.tsx"
related_targets: ["src/App.css","index.html"]
---

## Scope and mode

Operate-mode desktop shell for `src/App.tsx`, covering only the empty conversation, disabled talk control, and native runtime diagnostics.

## Audience and job

A B2 learner on a personal Mac needs to see that the local desktop runtime is ready before voice features are connected.

## Direction

Subtitled rehearsal. The approved comp is `.impeccable/mocks/subtitled-rehearsal-a.png`: a compact masthead, dominant cinema-safe dialogue stage, broad disabled action control, and subordinate system strip. The memorable moment is the large quiet stage framed like a place where spoken lines will appear.

## Implementation grammar

- Cool white and soft blue-gray surfaces, deep navy ink, signal blue for local identity, and semantic green/red only for health.
- Native system sans; subtitle-like centered empty copy; thin rules and corner markers instead of nested cards.
- Flat surfaces, 1px cool-gray rules, modest 12px corner language only on the talk control, and no decorative elevation.
- Semantic HTML/CSS owns every ingredient; no raster asset is required in the shipped UI.
- Fluid from 640×480 upward; compact windows reduce padding and stage markers without changing topology.

## Boundaries

No microphone behavior, Space shortcut, transcript turns, correction pane, settings, routing, remote assets, or invented product claims.
